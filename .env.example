# 复制到本机，不要提交真实值。
# cp .env.example apps/web/backend/.env.secrets
# cp .env.example 里的百度段到 apps/web/backend/.env.baidu

# 百度 OCR
BAIDU_API_KEY=
BAIDU_SECRET_KEY=
BAIDU_OCR_API=accurate

# MiniMax（可选）
MINIMAX_API_KEY=
MINIMAX_MODEL=MiniMax-M3
MINIMAX_ENABLED=false

# 飞书（第一期目标：网页应用身份；lark-cli 推送只作开发机调试）
FEISHU_ENABLED=false
FEISHU_OPEN_ID=
FEISHU_DISPLAY_NAME=
FEISHU_AS=bot

# 权限：公网/Tunnel 前必须为 true
AUTH_REQUIRE_KNOWN=true

# 可选：本机评测样本目录（不进 git）
# WB_SAMPLES_DIR=

# 上传上限 MB
# WB_MAX_UPLOAD_MB=200
