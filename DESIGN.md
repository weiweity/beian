# Design System — 江华审稿室

来源：Figma「审稿室 · iOS 27」锁定稿（2026-08-20）。  
稿：https://www.figma.com/design/BL3PGUjLGLPb9iUZMzRhD6  
页：`Web · 锁定稿`（封面 `87:1448`）。侧栏材质对照：`Web · 侧栏材质对照`（`98:1432`）。`iPad` 页是探索，**不是**实现依据。
资产：`apps/web/ui/public/brand/logo-mark.png`（侧栏）、`apps/web/ui/public/brand/shine-mage.png`（拒绝页整标）、`apps/web/ui/public/brand/ui/*.svg`（侧栏/上传/等待，从锁定稿导出）。

实现入口：`apps/web/ui`。不要再画飞书顶栏。不要重写审核引擎和 3D 流水线。

## Product Context

- **What this is:** 供应链内部飞书网页。一个工作场，两张台：审稿台（Excel↔备案/包装 PDF，人终审）、打样台（2D 刀模稿 → 可旋转 3D，检验结构）。另两个侧栏页：历史记录、设置。
- **Who it's for:** 上海 2–3 位同事。第一期用户刘籽烨。
- **Space/industry:** 包装审稿 / 刀模打样。不是 ERP、不是 BI、不是聊天、不是原生 App。
- **Project type:** 内部工具。飞书只坐身份。

记住的一句：**这是给人签字的审稿台。**  
8/31 只交审稿台。打样台可交平面稿出白底 / PPT / GLB；验收仍只看审稿台，不对业务承诺 Windows 打样。

## Aesthetic Direction

- **Direction:** iOS 27 Liquid Glass 网站。白底、浅雾、淡紫点缀。侧栏是玻璃，主区是工作台。
- **Decoration level:** restrained。紫只做选中框、主按钮、钉、等待强调。不要铺成品牌墙。
- **Mood:** 干净、可扫、字够大、苹果味。机器圈疑点，人写结论。
- **Reference:** 官方库 iOS and iPadOS 26（Figma kit：Liquid Glass / Sidebar）。iOS 27 收口：更透、折射、高光边、暗边。网站不是原生壳，玻璃用 `backdrop-filter` + 半透明白近似，不要假装能嵌入 Apple 组件。
- **不要：** 飞书顶栏、Ant 默认蓝、旧主色 `#722ED1`、深紫中台侧栏、纸台印泥、「AI 已过审」。

## Information Architecture

一间屋子，两张台，两个侧栏页。历史不是第三张台。

| 侧栏 | 里面有什么 |
|---|---|
| 审稿台 | 看板（审核单）→ 新建双井 → **核对页**（专属，一对一） |
| 打样台 | 上传平面稿；上传中、待打样与机器运行都留在台上，可搜品名/文件名并切换看板/表格 |
| 历史记录 | 审稿 + 打样混排，含进行中的单；可按类型、时间、生成人筛选，筛选后每页 10 行，并批量删除有权限的已结束单。在设置之上 |
| 设置 | 外观 / 开工板 / 费用 / 密钥。永远在侧栏最后一项。外观只写本机 `localStorage` 键 `wb_appearance`：主题、字号 px、`glassStyle`（`solid` / `frost` / `liquid`）、侧栏雾面对比度滑条、核对页差异标记 |

核对页是审稿台的工作页，不是新台签。点看板卡片进入。

## Typography

- **UI:** 系统中文栈。优先苹方 / 微软雅黑；Figma 用 Noto Sans SC 作稿。禁止 Google Fonts / Geist 网络加载。
- **Data/Tables:** IBM Plex Mono 或 ui-monospace，15–17px，tabular-nums — Excel/PDF 原文、条码、批号
- **Scale（自适应，不要太小也不要太大）:** 辅助 ≥13px，正文默认 16px，页名 28px，侧栏项 17px。控件高度 ≥40px。设置 · 外观 UI 字号是 13–28 的 px（滑条 + 可手写），锁定稿 16。
- **字重:** 页名 Bold/Medium，侧栏选中 Medium，说明 Regular

```css
--font-body: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei UI",
  "Noto Sans SC", "Alibaba PuHuiTi", sans-serif;
--font-mono: "IBM Plex Mono", ui-monospace, Menlo, Consolas, monospace;
```

## Color

Figma 变量集 `审稿室`（Light）：

| Token | Hex | 用途 |
|---|---|---|
| brand/purple | `#805898` | 选中框、主按钮、钉、等待数字 |
| brand/purple-soft | `#F6F1F8` | 选中底、当前疑点 |
| brand/purple-mist | `#EDE7F1` | 浅雾、弱分割 |
| surface/bg | `#F8F5FA` | 页底 |
| surface/white | `#FFFFFF` | 主区 |
| text/ink | `#1C1A1F` | 正文 |
| text/mute | `#6B6572` | 次要 |
| line/subtle | `#E7E2EC` | 线 |

- **Semantic:** 一致可用绿字+汉字；疑点/待人工确认 `#D48806`；缺失 `#F5222D`。禁止只靠色点。
- **Hover 灰:** `#E2E2E8`（侧栏项 Hover）。
- **飞书头像底（无图时）:** `#3370FF`。
- **Dark mode:** 默认浅色。设置 · 外观可改浅色 / 深色 / 跟随系统。深色不是灰黑中台：页底 `#141018` 紫黑，主台 `#221B28`，字 `#F6F1F8`（品牌 purple-soft），点缀仍是 `#805898` 提到 `#C9A3D6`。字重 500，避免细灰字。只写 `localStorage`。
- **外观控件（iOS 27）:** 离散项用 Segmented（主题、侧栏材质、差异标记）；连续项用苹果滑条（侧栏对比度、字号）+ 可手写 px。对比度只调毛玻璃和液态玻璃的雾面：低更透、高更实。实心时滑条无效。写入 `localStorage.wb_appearance.glassStyle` = `solid` / `frost` / `liquid`。旧 `glassSidebar` 布尔会迁：关→实心，开→毛玻璃。

Ant Design 6 只当零件箱：

```
colorPrimary            #805898
colorSuccess            #389E0D
colorWarning            #D48806
colorError              #F5222D
colorBgLayout           #F8F5FA
colorBgContainer        #FFFFFF
colorText               #1C1A1F
colorTextSecondary      #6B6572
colorBorder             #E7E2EC
borderRadius            16
fontSize                16
controlHeight           40
```

## Spacing

- **Base unit:** 8px
- **Density:** comfortable（大字优先）
- **Scale:** 2xs(2) xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48)
- **壳层内边距:** 页 20px；侧栏 20×16；主区标题到内容 16

## Layout / Chrome

- **Approach:** iOS 27 分栏：侧栏贴窗口左边，主区连成同一块工作台。不是两张漂浮卡。页底淡紫雾；`.workspace` 一块圆角 28 的窗口（`--stage`）。aside / main 之间只有一根 0.5px `--hairline`（`ink` 10%），只要隔开。
- **滚动:** 壳 `100dvh` `overflow: hidden`。侧栏不 sticky、不跟页滚。只有 `.stage`（设置页是内部 `.settings-main`）滚动。`backdrop-filter` 只留在侧栏，避免双滚动卡顿。
- **展开侧栏:** 280px。玻璃层宽度锁在 280px，折叠只裁切窗口（iOS 27 sidebar 贴边）。工作台窗口底透明，让壳上的紫雾透进侧栏；主区 `.stage` 仍是白紫台。打样台和打样单 `.stage` 是纯白 `#ffffff`、深字（深色主题也是）。三档材质：实心 = 不透灰紫 `purple-mist`；毛玻璃 = 现在的雾面 `blur(24px) saturate(180%)` 叠 `rgba(255,255,255, var(--glass-alpha))`；液态玻璃 = iOS 26 kit Sidebar / Liquid Glass 的网站近似，并按 iOS 27 收口（更透、折射环境紫雾、`blur(40px) saturate(240%)`、高光边、暗边、右侧色散细线）。外观「对比度」0–100 无极调 alpha（55 对准锁定稿 0.55）；液态玻璃 CSS 浅色乘 0.42、深色乘 0.5，看起来更透。系统开「减少透明度」时，毛玻璃和液态玻璃（浅色和深色）降到 `blur(12px) saturate(140%)`；毛玻璃只降 blur，液态玻璃底更实，高光/色散层和折叠钮玻璃关掉。对照稿：Figma `Web · 锁定稿` → `Web · 侧栏材质对照`。词标 `SHINE MAGE` 一行 nowrap，展开时宽度够了再淡入，避免先露出 SHINE。
- **折叠侧栏:** 76px。图标 44pt 热区，垂直居中。
- **折叠顶身份:** 默认 `logo-mark`；悬停变成展开按钮（sidebar.right）。同一 44pt，280ms Gentle 交叉淡入。不要把折叠按钮和 logo 做成两个热区。
- **展开顶品牌:** 左 `logo-mark` 36px +「SHINE MAGE」13px tracking 0.6px；右独立折叠按钮（sidebar.left）44 玻璃圆。
- **侧栏项:** 248×56（展开时撑满内宽），圆角 12px，图标 **24px** + 17px 文案（锁定稿图标 22px，实机偏小故加大）。Hover 浅灰块；Selected **2px** `#805898` 框 + `purple-soft` 底 + 紫字。不要紫胶囊填满。
- **顺序:** 审稿台 → 打样台 → 历史记录 → 设置。
- **账户行:** 左下角。飞书头像 **36px** + `花名（真名）` **15px**（锁定稿 28/13，实机过小）。例如 `天元（魏炜）`。来自登录态，不要写死。点开可退出。无花名时只显示真名。当前 `vX.Y.Z.W` 用 11px 弱化文字紧跟在姓名下面，不单独漂在账户行上方；只读已登录 `/api/status.version`，前端不维护第二份版本号。
- **主区页名:** 28px。说明 16px mute。主按钮 40×圆角 20，实心紫，一屏尽量只留一个实心紫。
- **看板:** 审稿台四格 2×2 玻璃卡 圆角 26：对照中 / 对照失败 / 待审核 / 已签字。四格都用紧凑明细：首行尽量只放品名 + 状态；对照中改为品名 + 阶段百分比，下一行只放日期。打样台三列：打样中 / 打样失败 / 已出图。卡片以品名为主，右侧状态（含「待确认结构」）允许换行但不能遮住品名。列高锁视口，卡多了列内滚动，右上「全部」开该列明细表。等待卡和历史行仍写真实阶段/大约还要，不画假精确百分比。侧栏审稿台/打样台作业在跑时有圆点。右上「进入工作台」才去上传页。
- **新建:** 品名与包装面同一行；Excel / 包装 PDF **左右双井**，虚线 1.5px `rgba(128,88,152,0.28)`，圆角 22。底下三步：传一对 → 机审标疑点 → 她来签字。上传卡按「已选文件 → 上传到服务器 → 服务器确认 → 待开工」显示真实状态：分片字节进度、自动重连次数、100% 后的服务器确认、成功、已暂停和失败。SPA 换台与整页刷新都能找回服务端会话；暂停后重新选择同一文件从已确认偏移量继续。主动作只在回执 ready 后可点，暂停态提供「继续上传」与「删除暂存」，不把 100% 误写成上传成功。
- **打样台:** 同一玻璃语言，一个大井只交 `.ai`。不要做成审稿的双井。不要再收平面 PDF。打样台只交稿和列单；点进度或已出图进单独打样单。网页新打样固定走 V2，结构状态必须分出「识别结构中 / 待确认结构 / 当前不支持 / 打样中」；有歧义时展示最终预检通过的真实完整展开图，不把零散矩形、尺寸、封口算法或 CAD 术语交给用户。多方案只用「上一张 / 下一张」做视觉切换；已登录的 admin / reviewer / viewer 在 A–D 中选择印有品名和主视觉的产品正面并点「生成打样图」，阅读方向由确认引擎的几何建议自动带入，右侧、反面、左侧、顶部、底部仍由连通关系推导。未登录仍 401；viewer 仍不能新建打样。预览必须画真实多边形，不得用 bbox 假画形状。显式 `packaging:cut` / `packaging:crease` 语义直接进入拓扑验证；没有对象语义的旧 AI 先盘点绑定当前源稿的纯描边候选层，已登录账号在同一打样单中选择真实刀线、折线所在的一层或多层后重新识别。唯一「上刀线」（或单层「刀线」）默认黑盒提交识别，候选里唯一「印刷」可一并带上，失败才露出选层；唯一「刀线」旁边还有「折线」时仍要人勾。Illustrator 同时导出与源稿、画板坐标和候选 ID 绑定的有界矢量预览；网页用最高 5600px、32MP 封顶原稿作底图，鼠标移入临时涂亮、勾选后持续涂亮真实直线/贝塞尔路径，并提供 100%–600% 放大。紫色只表达当前人工选择，不成为结构语义或颜色推断。旧任务没有该快照时保留高清原稿降级，不伪造路径。首次进入只显示一次非阻断引导浮层「选图层 → 看左侧涂亮 → 点右下角重新识别完整盒型」，不叠第二个确认弹窗。界面只提交候选 ID，服务端校验权限、源稿与候选成员；所选线稿仍必须形成完整网。任何路径都不得按颜色、普通 OCG、图层名、间距、bbox 或模板自动接受结构（黑盒提交仍只发候选 ID，服务端校验后重跑，不把图层名当成 cut/crease）。桌面宽度 >1024 时，待确认结构是填满剩余工作区的单屏：真实稿、两步引导、图层或正面选择、可恢复错误和主动作必须同时可见，不叠第二层确认弹窗；窄屏才改为纵向并允许主区滚动。打样台和打样单主区白底深字（`#ffffff`，不是紫雾台；深色主题也是）。打样单一屏三列等高：正面+侧面、反面+侧面、GLB，用来看形；白底静帧用浅底+内描边，避免白盒看不见边；页头灯光滑条同时调静帧亮度和 GLB 曝光，不重跑 Blender。其下是读字区，至少正面/反面印刷面，来自已生成的 `panel_*.png`，1–6×，不是 GLB 截屏。每张图右上角 44 玻璃圆钮下载。页头右上「下载 PPT」（没写成 PPT 仍显示，点了出「PPT 没写成，白底仍可下」）。不下 PDF。点下载出底部玻璃条「正在下载…」，点不着、不挡手。GLB 全屏后水平垂直居中；截图时下载钮藏起来；全屏被拒出「全屏打不开」。GLB 中性环境、曝光跟静帧，接触阴影。已经出过的白底图要重新打样才会变。打样单 PDF 槽按源图比例 contain，不拉长。

- **核对页（专属）:** 画布吃满宽度。页图 contain 靠右（左边留给核对窗，不随拖窗挤压），优先加载可无损放大的矢量核对面；矢量生成或加载失败时自动退回高清 PNG。初始 1× 状态不施加恒等 CSS transform，也不常驻 `will-change`；仅在实际平移时短暂启用合成层，避免浏览器先把高清 SVG 栅格化成模糊缩略图。OCR 矩形框和编号钉分开关；拖动画布时框暂时隐藏。工具条可收缩，停在画布右上。拖动画布（不要地球图标）、滚轮/按钮缩放，点核对窗序号后局部放大该框。核对窗 portal 到整页应用顶层（高于侧栏、页头、签字和画布），进页展开并叠在左侧栏上，可拖到视口内任何位置，不挤页图；窗内 Select 等弹层再高一层。收起/展开 280ms Gentle；展开和收起分别按当前可见尺寸夹在视口内，持久化视口 `top/left`，不设页头下方的空气墙；边框上下左右和四个斜角都能缩放。疑点列表横向置顶。当前字段先用 16px 正文写嘱咐（lead / because / ask），结论下拉紧贴 ask，再两列「Excel 应印｜稿上读到」（命中比只在「查看全部」里写成「12 个词读到 3 个」），窗底写这单还剩 M 条、本页还有 X 条。钉号等于疑点序。一致/有错/忽略用下拉框，右侧是复制，下一行是人工备注输入。结论仍在页头签字旁填写。默认只亮当前字段的钉和框，永不恢复全页散钉；工具条开关只作用于本字段。面积 ≥ 页 30% 的框当未定位，顶栏写「这条我钉不住，请整面看」。进页先翻到未决疑点，图就绪后再局部放大。点字段，图上跟到这一条。二维码只按完整扫码关注引导语核对，不用解码内容替代引导语或定位框。工艺说明 / 颜色要求 / 版本号 / 更新内容不进机审（只认字段名开头，不误杀「备案版本号」「执行标准版本号」）。
- **地址:** 一间屋子多条路径。`/reviewup` 审稿台看板，`/reviewup/new` 审稿工作台，`/review/:id` 核对页，`/mockup` 打样台看板，`/mockup/new` 打样工作台，`/mockup/:id` 打样单，`/history`，`/settings`。旧 `/` `/new` `/review` 302 到新地址。浏览器后退换台，不要把整站钉在 `/`。
- **等待卡:** `WaitLoader` 等待圆盘（对照中 / 对红中 / 打样中）自适应尺寸 + 大号 ETA + 阶段胶囊 + Apple 式进度条；不要出现英文 `Generating`。三种：对照中 / 对红中 / 打样中。排队只写「前面还有 N 单」或「就快轮到」，不准写 40 秒。跑起来 ETA 用大约：对照/对红约 40 秒，打样约 4 分钟；≥60 秒写分钟。离开后再进审稿台看板、打样台或历史记录，仍能看到阶段。点打样进度或历史记录里进行中的单，进打样单等待卡，不要在打样台底下摊开。**不要做假取消**——后端取不消对照/Blender 就不要写「取消」。
- **登录:** 没有自建登录页。未登录直接去飞书官方授权。只放行伸美企业（`tenant_key`）。拒绝页：完整 `shine-mage.png` +「进不了这间审稿室」+ 原因 +「再试一次」。
- **窄屏:** ≤1024 默认折叠侧栏（折叠轨 `39:1116`）。390 一列看板。展开不挤主区：≤720 用 Figma `32:1081` 玻璃层盖在主区上，点遮罩收回。台签不拆字。液态玻璃抽屉盖在主区上（含核对页）时改更实雾面并关掉色散边，避免底下的字糊进侧栏。
- **Max content:** 审稿画布吃满剩余宽度
- **Border radius:** 钉 9999px，侧栏项 12，井 22，玻璃卡 26，侧栏 28，主按钮 20

## Motion

- **Approach:** Apple 味、短、可关
- **Easing:** Gentle（接近 ease-in-out）
- **Duration:** 折叠身份 280ms；核对窗收起/展开 280ms；侧栏项 hover 150–180ms；等待圆盘慢转、字母弹跳
- **尊重** `prefers-reduced-motion`
- 登录无自建动效。未登录即跳飞书授权页。

## 文案与状态

| 状态 | 她看见 |
|---|---|
| 未登录 | 直接去飞书授权。非伸美企业：logo +「进不了这间审稿室」+ 原因 +「再试一次」 |
| 无任务 | 不画空表、不画空列「没有单」。加载中也不抬看板。卡片：「还没有审核单」+「右上角进入工作台」 |
| 机审中 | 等待卡「对照中」。排队写「前面还有 N 单」；跑起来写「大约还要 40 秒」（≥60 秒写分钟）。机审只标疑点，不会自动过审 |
| 对红中 | 等待卡「对红中」。排队同上。对红失败仍可签字 |
| 待审核 | 核对页：优先矢量核对面，失败自动退高清 PNG；初始 1× 不施加恒等 transform；只亮当前字段的真框 + 钉在框中心 + 应用顶层横向核对窗（疑点列表置顶，16px 嘱咐再结论下拉，Excel 应印与稿上读到两列；进页展开叠在左侧栏，可拖到视口任何位置，不挤页图；展开/收起按当前尺寸碰撞，边框八向缩放）；钉和框只开关本字段；没有位置就不画假钉，写「这条我钉不住，请整面看」；OCR 不清必须写「待人工确认」 |
| 她提交 | 「已记录你的结论，不是系统过审」 |
| 待确认结构 | 桌面单屏展示最终预检通过的真实完整展开图、两步引导、产品正面 A–D 和「生成打样图」。多方案只做视觉翻页，不显示完整盒型尺寸、封口说明或阅读方向；系统带入几何推导的默认朝向。已登录的 admin / reviewer / viewer 都可选择和生成；未登录仍 401。其余五面仍由连通关系推导并严格复核；失败在原位说明且可重试，不弹第二层确认框、不进入 Blender |
| 当前不支持 | 说明具体原因和下一步。旧 AI 无对象语义但存在纯描边候选层时，已登录账号在高清原稿旁多选本稿候选；唯一「上刀线」（或全单只剩一层「刀线」）默认黑盒提交识别，候选里唯一「印刷」可一并带上，失败才露出勾选。旁边还有「折线」的「刀线」仍预勾，人可改。移入或勾选一层会涂亮 Illustrator 导出的真实路径，确认后点右下角「重新识别完整盒型」。烫雅银等填色工艺板单独列出，只供涂亮查看，不能提交进拓扑。工艺板与刀线分预算，坏板或对不上号的涂亮层单独跳过，不关整份刀线涂亮；超大或非法几何才关掉整份预览，选层入口仍在。没有矢量快照时只降级原稿并提示重新识别，不伪造涂亮；没有候选时才提示用 `packaging:cut` / `packaging:crease` 标记。所选线稿仍要形成完整盒身和上下封口；曲线、单位、拓扑或数量越界写对应原因，不自动降级套模板 |
| 打样中 | 结构已批准后才显示等待卡「打样中」。排队写「前面还有 N 单」；跑起来写「大约还要 4 分钟」，阶段芯片「出图 / 打样 / 导出」。本机 Blender。白底不要带尺寸标注再交备案。离开再回看板、打样台或历史记录仍能看到阶段。点进度或已出图进打样单（三图看形，下面读字），不要在打样台底下摊开 |
| 打样台可跑 | 选稿 → 结构语义/拓扑闸门 → 必要时确认完整盒型 + 正面锚点 → 自动推导五面并严格复核 → 跑 worker → 点进打样单一屏三图：正面+侧面、反面+侧面、GLB 看形，下面印刷面读字。白底静帧浅底+内描边，GLB 框不画内描边；页头灯光滑条同时调静帧亮度和 GLB 曝光，不重跑 Blender。主区白底。每张图右上角 44 玻璃圆钮下载；页头「下载 PPT」（没写成仍显示，点了出「PPT 没写成，白底仍可下」）。不下 PDF。点下载底部玻璃条「正在下载…」，点不着。GLB 全屏居中；截图时下载钮藏起来；全屏被拒出「全屏打不开」。PDF 槽 contain。PPT 用两张白底写 OOXML，不依赖 Node；写不出才试演示文稿运行时。白底仍合成 PDF（页底先铺白；pymupdf 写不出且还没落盘才用已装 Pillow，不盖掉已写成的文件）。失败写清缺 Blender 或结构语义/拓扑问题 |

禁止：「已过审 / 已发送 / AI 已确认」。禁止达肤妍吉祥物当本产品品牌。

## Logo

- 侧栏：`logo-mark.png`（狐狸头，光学居中 48×48）。折叠/展开都用它，不要改回完整词标占顶栏。
- 拒绝页：完整 `shine-mage.png`（狐狸头 + SHINE MAGE），高度约 32px。
- 等待：`WaitLoader` 圆盘显示中文状态（对照中 / 对红中 / 打样中），不要出现英文 `Generating`，也不要换成 Grok 官方双眼标。Figma 狐狸球 node 只作历史对照。
- 不要再手写中文词标，不要黑底矩形。

## Figma 对照（实现时打开对应 node）

展开

- 审核单 `28:956`
- 审稿 `32:967`
- 新建 `32:1005`
- 1024 `32:1043`
- 390 `32:1081`

折叠

- 侧栏折叠 `39:1116`
- 折叠悬停 `71:1321`

工作

- 核对页 `46:1021`
- 对照中 `68:1515`
- 打样台 `51:1362`
- 打样中 `68:1660`
- 历史记录 `76:1443`

组件

- 侧栏项 `58:1091`
- 折叠身份 `71:1313`
- 账户行 `73:1323`
- 狐狸球 `67:1167`
- 等待卡 `68:1218`
- 任务行 `31:956`

## Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-08-18 | 一间审稿室、两张台 | 用户锁定：一个工作场，审稿 / 打样 |
| 2026-08-18 | 放弃纸台印泥 | 当时要紫调、白底、大厂风 |
| 2026-08-19 | 顶栏用完整 logo | 当时不要只放头 |
| 2026-08-19 | HTTP 产品层 TypeScript | 对照/Blender 仍是 Python worker |
| 2026-08-19 | 登录定稿 A 飞书 SSO | 进站即飞书授权；只放行伸美 tenant |
| 2026-08-19 | 看板 + 费用 C 档 | 审稿三栏看板；设置开工板；厂商余额 + 本机台账 |
| 2026-08-20 | 锁定 iOS 27 玻璃侧栏 | 网站不是原生；Liquid Glass；logo-mark；淡紫点缀；不要飞书顶栏 |
| 2026-08-20 | 核对页专属 | 一对一定位，不是列表上的小窗 |
| 2026-08-20 | 历史是侧栏 tab | 审稿+打样混排，不是第三张台，不放在打样台下面 |
| 2026-08-20 | 账户行花名（真名） | 飞书头像 + 天元（魏炜）；无花名则真名 |
| 2026-08-20 | 折叠身份 hover-morph | logo-mark 与展开按钮同一 44pt，280ms |
| 2026-08-20 | 等待用狐狸球 | logo-mark + Grok Bot 轨道语言；ETA 用大约 |
| 2026-08-20 | Web 为实现依据 | iPad 探索页不指导 `apps/web/ui` |
| 2026-08-21 | aside+main 连成窗口 | iOS 27 侧栏贴窗口边；发丝线隔开；玻璃层固定宽度裁切，修展开残影 |
| 2026-08-21 | 390 展开走 Figma 层 | 锁定稿 `32:1081` 是整宽玻璃侧栏，不是 280 挤主区。默认仍折叠 `39:1116` |
| 2026-08-20 | 主区淡白板块 | Figma Body/Main 是白台，雾只做页底；壳 100dvh 隔离滚动 |
| 2026-08-20 | 空审核单不闪三栏 | 初次点审稿台、加载中都不画「没有单」列 |
| 2026-08-20 | 设置 · 外观 | 主题/字号/玻璃侧栏/对比度/差异标记；参考 Codex 行式控件 |
| 2026-08-23 | 侧栏三档材质 | 实心 / 毛玻璃 / 液态玻璃。现网半透明=毛玻璃；液态玻璃对照 iOS 26 kit + iOS 27 高光边/暗边。网站近似，不嵌 Apple 组件 |
| 2026-08-23 | 降低透明度 | 系统 `prefers-reduced-transparency` 时毛玻璃只降 blur(12px)；液态玻璃（含深色）底更实，色散边和折叠钮玻璃关掉 |
| 2026-08-23 | 窄屏液态抽屉 | ≤720 展开盖主区时液态玻璃改更实雾面，避免底下的字糊进侧栏 |
| 2026-08-24 | 核对窗液态玻璃 | 悬浮窗本身是液态玻璃，可缩放；结论在页头签字旁，不在窗底 |
| 2026-08-24 | 隐藏钉只藏编号 | 定位方框留下，编号圆圈可关 |
| 2026-08-24 | 显示框可关 | 钉旁单独开关定位框；拖动画布暂时藏框；工具条可收缩 |
| 2026-08-24 | 核对窗整页置顶 | 悬浮窗不在图内，钉在整页右上，可拖、四角缩放 |
| 2026-08-24 | 核对窗八向缩放（已被 8/30 顶层视口模型取代） | 旧方案默认停在页头下面；只保留八向缩放的交互依据 |
| 2026-08-24 | 打样台与打样单分开 | 台面只交稿列单；点进度/已出图进单独打样单看白底和 GLB |
| 2026-08-24 | 打样按已登记刀模匹配（已被 V2 取代） | 旧方案仅留迁移追溯，不再作为新任务的自动判断 |
| 2026-08-24 | 打样跟刀线（已被 V2 取代） | 旧方案无法证明线条语义；V2 不再按图层名/颜色/间距猜刀线 |
| 2026-08-27 | PackagingStructure V2 | 显式语义 → Shapely 拓扑 → 必要时确认完整盒型与正面锚点 → 自动推导五面 → 现有 Blender；无语义或不安全结构不得进 Blender |
| 2026-08-27 | 核对面优先矢量 | 小字放大保持清晰；独立子进程失败或浏览器加载失败时自动退高清 PNG，不让核对页空白 |
| 2026-08-27 | 核对窗横向证据带 | 疑点列表置顶，当前字段按疑点 / Excel / OCR 三列对照；定位、下拉结论、复制和备注收在下方 |
| 2026-08-24 | 核对窗盖住侧栏（已被 8/30 顶层视口模型取代） | 旧方案只允许盖侧栏；portal 方向保留 |
| 2026-08-24 | 多路径换台 | `/` `/new` `/review/:id` `/mockup` `/mockup/:id` `/history` `/settings`，后退换台 |
| 2026-08-25 | 审稿台 /reviewup | 看板 `/reviewup`，工作台 `/reviewup/new`；核对页仍 `/review/:id`。旧 `/` `/new` `/review` 302 |
| 2026-08-24 | 打样单三图 | 一屏左到右正面+侧面、反面+侧面、GLB；图上下载；返回下下载 PDF |
| 2026-08-25 | 核对窗默认叠左侧栏（层级已被 8/30 取代） | 默认位置与 280ms Gentle 保留；不盖签字的层级限制取消 |
| 2026-08-25 | 打样台/打样单白底 | 主区白底；GLB 中性环境、曝光收一点；旧白底图要重打才会变白 |
| 2026-08-25 | 打样 PDF 铺白+Pillow | pymupdf 先铺白再贴图；写不出且还没落盘才用已装 Pillow，不盖掉已写成的文件 |
| 2026-08-25 | 核对窗剩余区（已被自由浮窗取代） | 旧碰撞方案只作历史追溯，不再约束当前视口模型 |
| 2026-08-25 | 核对窗不挤页图 | 窗脱离页面随意拖，页图不被挤压（不再进剩余区） |
| 2026-08-30 | 核对窗顶层视口模型 | portal 到应用顶层，可覆盖页头/签字/侧栏/画布；以当前展开或收起尺寸约束 `top/left`，可拖到视口底部；窗内弹层再高一层 |
| 2026-08-30 | 核对图首屏高清 | 初始 1× 输出 `transform: none`；仅实际拖动画布时临时使用 `will-change`，不靠全屏触发重栅格化 |
| 2026-08-30 | 待确认结构单屏 | 桌面并排压缩为一屏，主动作和原位错误始终可见；取消重复确认弹窗，窄屏才纵向滚动 |
| 2026-08-30 | 版本随账户 | 服务端版本紧跟在姓名下面；折叠侧栏时与姓名一起隐藏 |
| 2026-08-31 | 点哪亮哪 + 同事口吻 | 默认只画当前字段钉框；嘱咐 16px lead/because/ask；命中比进查看全部；钉号=疑点序；法规小字仍阻断签字 |
| 2026-09-01 | 打样单团队可读、确认独立授权 | `admin/reviewer/viewer` 都可查看团队打样单；`confirm_structure` 保护确认动作；删除仍按本人或管理员 |
| 2026-09-02 | 已登录即可生成打样 | `confirm_structure` 发给 `admin` / `reviewer` / `viewer`；未登录仍 401。viewer 仍不能新建打样 |
| 2026-09-01 | 待确认结构客户化 | 只看真实展开图、选产品正面、生成；多方案视觉翻页，系统内部决定预检通过的默认朝向，不展示 CAD 参数 |
| 2026-09-01 | 历史记录十行分页 | 审稿、打样和上传合并筛选后再分页，每页固定 10 行；批量全选只作用当前页 |
| 2026-09-01 | 旧稿结构层人工选择 | 无显式语义但有纯描边候选时，已登录账号只提交本稿候选 ID，多层可一起选；服务端绑定源稿后重跑，完整盒型闸门不变 |
| 2026-09-02 | Illustrator 分层涂亮预览 | 候选盘点同步输出有界真实路径快照；高清底图支持 1–6×，移入临时亮、勾选持续亮；一次性非阻断指引把主动作固定在右下角，颜色不参与识别 |
| 2026-09-02 | 涂亮预算隔离 | 刀线与工艺板分预算；坏层跳过不关整份刀线预览 |
| 2026-09-02 | 打样单读字区 | 3D 三图只看形；读字用已生成的各面印刷 PNG（`read_front` 等），1–6×，不用 GLB 读小字。旧单磁盘上已有 `panel_*.png` 的直接露出，没有则重新打样 |
| 2026-09-02 | 默认结构层黑盒 | 唯一「上刀线」（或单层「刀线」）自动提交识别，候选里唯一「印刷」可一并带上；失败才露出选层。工艺板仍不能当刀线 |
| 2026-09-02 | 打样单描边与灯光 | 白底图浅底+内描边；灯光滑条同时调静帧亮度和 GLB 曝光，不重跑 Blender |
| 2026-08-25 | 字段顶栏疑点 | 有 miss 或她标有错时先写「疑点 / 错误点」，一致且无 miss 不占地方 |
| 2026-08-25 | 打样下载圆钮 | figcaption 右侧 44 玻璃圆钮，不压产品；PDF 槽按源图比例 contain |
| 2026-08-25 | 打样图右上下载 | 三图等高对齐；下载在图右上角；页头「下载 PPT」（没 PPT 仍显示，点了出「PPT 没写成，白底仍可下」），不提供 PDF 下载入口；底部玻璃条不挡手；GLB 全屏居中；截图藏下载钮；全屏打不开 |
| 2026-08-25 | 棚只提亮 | add_studio 只提亮和接触阴影；涂层/UV 在 add_box，未书面允许不改 |
| 2026-08-20 | 外观连续量 | 对比度=侧栏雾面滑条；字号=可手写 px；深色走品牌紫雾 |
