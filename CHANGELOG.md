# Changelog

## [0.20.6.0] - 2026-08-31

### Changed

- 待确认结构改为桌面单屏工作区：真实稿、盒型/正面/阅读方向、自动推导摘要、原位错误与「确认并开始打样」同时可见；移除重复确认弹窗，并用同步提交锁防止双击产生并发请求。
- Packaging Structure V2 把尺寸精度收口为适配器级合同：四个盒身面决定成盒尺寸，顶底封口让位只做闭合验证；盒盖贴图方向改由唯一共享折线中点与盒盖质心推导。
- 侧栏版本号移到登录姓名下方；打样卡片状态允许换行，避免遮住品名。

### Fixed

- 修复旧刀线提案在 1.5 mm / 3% 规则下生成、却在确认和解析阶段按更严规则拒绝，最终误报“盒盖折线不能唯一确定贴图方向”的跨阶段策略漂移。
- 修复打样开工和结构确认失败后 Promise 未处理、重复 400、过期回执仍被前端复用的问题；失败现在留在原位，可修正或刷新回执后重试。
- 修复回执原子领取后、任务 JSON 落盘前进程退出会永久丢失待开工上传的问题；durable claim 会在启动时按持久化任务事实恢复或清理，同一回执的并发开工也会收敛到同一任务，过期判断改用稳定错误码。
- 修复核对页首屏 SVG 因恒等 transform 与常驻 `will-change` 被提前栅格化而模糊，以及悬浮窗受页头层级和展开尺寸碰撞约束、收起后无法拖到底部的问题。

### For contributors

- L0 新增同一上传回执并发开工幂等、结构确认失败可重试且无 page error、桌面 CTA 在视口内、核对窗应用层级/收起后底边拖拽、初始无 transform，以及真实封口让位盒型的回归；真实杭州 Illustrator/Blender 仍属于合入后的 L1/L2 验收。

## [0.20.5.0] - 2026-08-30

### Changed

- 飞书与本机显示名会话统一改为每次请求重新校验 `users.json`：授权只认真实 `open_id` 或唯一显示名与显式角色绑定；账号删除、停用或角色调整会立即撤销或同步既有会话。
- 项目级 Ant Design skill 改为仓库内 vendored 权威源，并由正向许可、内容哈希和只读 CLI 包装器共同约束；普通产品任务不能自动安装、升级或绕过包装器执行工具命令。

### Fixed

- 移除按姓名或花名自动提升管理员的路径；重复身份、未知角色和不可读账号目录现在失败关闭，公网或非 loopback 请求会清除旧显示名会话，显式退出不再依赖账号目录可读。
- 持久会话恢复现在逐字段校验 token、来源、角色、身份字段和时间戳；同时修正带端口的 IPv6 loopback（如 `[::1]:8787`）识别，畸形 Host 不再误入本机显示名边界。
- 修复项目 skill 锁为空、条目为 `null`、存在额外目录/锁项、符号链接或远程来源时可能无法形成完整供应链证据的问题；锁条目不是对象时也会明确失败。

### For contributors

- L0 增加真实 Hono 身份边界、会话恢复/降权/撤销、项目 skill 哈希与策略、只读 Ant Design CLI 参数边界测试；Mac 测试与构建不替代合入后绑定 merge SHA 的杭州 Windows L1 和公网版本核对。

## [0.20.4.0] - 2026-08-30

### Changed

- Illustrator 桌面代理在读取命名管道请求、等待共享执行锁和运行桥接进程期间统一每 5 秒续写心跳；Hono 与 Python 客户端统一按 30 秒过期、5 秒未来时钟偏差判断在线状态。
- 杭州交互桌面的持久 Agent 异常退出后改为每分钟重试、最多 60 次；临时 L1 Agent 仍只重试 3 次且受绝对到期时间约束，注销后仍须管理员重新登录。

### Fixed

- 修复 Agent 合法繁忙期间心跳未及时刷新、打样开工被误判为“桌面代理已离线”并返回 412 的问题；过期心跳仍会失败关闭，并保留已上传回执供恢复后重试。
- 锁定打样开工的写请求合同：一次调用只发送一个 `POST /api/mockups/start`，412 不会被 API 层自动重放。

### For contributors

- Node 22 L0 覆盖服务端 352 项、界面 263 项、Python 270 项，以及类型、构建和 Knip/Vulture 门禁；PowerShell 5.1、InteractiveToken、计划任务重启与管道→VBS→JSX 仍须由合入后绑定 merge SHA 的杭州 L1 和公网版本核对证明。

## [0.20.3.0] - 2026-08-30

### Changed

- 发布排空从 Hono 中间件里的标量计数与 `Response.body` 包装器，重构为 Node `fetch` 最外层的 `ReleaseCoordinator`：每个业务请求都有独立 Map 记录，并分别等待 handler promise 与 `ServerResponse finish/close/error` 两个生命周期闩锁。
- 受令牌保护的 release control 新增脱敏请求诊断，只返回请求编号、方法、路由类别、年龄与闩锁状态；发布脚本继续只消费稳定的 `active/ready/blocker_codes`，不需要理解 URL 或业务内部结构。

### Fixed

- 修复客户端中断压缩请求后，`@hono/node-server` 因 writable 已销毁而不再读取或取消响应 Web Stream，导致 admission token 永久不释放、杭州发版持续被幽灵 `requests_active` 阻塞的问题；真实下载仍会保持 active，直到传输和 handler 都结束。

### For contributors

- L0 新增真实 TCP 回归：8 MiB 下载在客户端未读完时必须阻止排空；gzip 请求在 handler 返回前断开 socket 后，先记录传输终止、再等 handler 收尾并精确归零。服务端、界面、Python worker、类型、构建与 Knip/Vulture 门禁均在 Mac 验证；杭州 Windows 发版事务和公网版本仍须合入后的 L1/canary 证明。

## [0.20.2.1] - 2026-08-30

### Added

- 打样结构确认新增完整六面盒网提案：系统先从刀线层识别四个连续盒身面与两个封口面，过滤内嵌尺寸框、参考矩形和不完整结构；存在多个完整方案时，管理员只需先选择整套盒网。
- 侧栏左下角新增服务端版本展示，直接读取当前登录态的 `/api/status.version`，不在前端维护第二份版本常量。

### Changed

- 结构确认收敛为一个最小锚点：选择产品正面和 0/90/180/270° 阅读方向后，右侧、反面、左侧、顶部和底部由折叠连通关系自动推导，不再要求用户逐面填写六个角色。
- 结构预览叠加真实 artwork 与真实多边形，零散矩形不再进入可选列表；完整网不唯一、折叠关系不成立或方向无法证明时继续失败关闭，不会回退颜色、图层名、间距、 bbox 或模板猜测。

### Fixed

- 修复封口面仅按长宽推断方向时，方形顶底面可能旋转错误的问题；现在依据实际共享折线、连接的盒身角色和 Blender 面轴合同推导纹理方向。
- 修复重复路径可能制造重复盒网、异常候选导致搜索失控，以及伪造或过期锚点可能越过结构确认边界的问题；搜索有确定性上限，公共接口只保留与当前源稿绑定的合法完整盒网。

### For contributors

- L0 覆盖横向/纵向盒网、重复与近重复路径、全部封口连接侧、方形封口、非法锚点、旧任务兼容和浏览器完整流程。Mac 合成验证不替代杭州 Windows Illustrator/Blender L1，也不替代人工核定真实稿的 L2 金标验收。

## [0.20.2.0] - 2026-08-30

### Fixed

- 修复杭州 Windows PowerShell 5.1 调用 `.NET File.Replace` 时把 `$null` 备份路径绑定为空字符串、导致发版在停服前报“路径格式不合法”的问题；发布、独立恢复和 Illustrator Agent 的 6 个原子替换点统一传入真正的 CLR null。

### For contributors

- 新增 GitHub-hosted `windows-2022` PowerShell 5.1 合同测试，真实验证无备份文件的原子替换语义；静态门禁同时锁定全部 6 个调用点，禁止回退到 `$null`，且不使用杭州 self-hosted 生产 runner。

## [0.20.1.0] - 2026-08-29

### Fixed

- 修复杭州 Windows PowerShell 5.1 将原生 `git` 实时输出接入截断管道后，把成功命令误判为 `LASTEXITCODE=-1` 的问题；发布、独立恢复、Illustrator Agent 安装/运行与 L1 smoke 现在都先完整收集输出并立即保存退出码，再解析单行结果。
- 修复发版 journal 尚未落盘时的清理异常覆盖首个失败原因；预事务清理现在只处理本次发版拥有的明确文件，删除失败仅告警并保留原始发布错误。
- 首次 `0.19.x → 0.20.x` 维护窗授权改为受控代际范围，使 `0.20.0.0` 在停服前失败后可由仅含发布修复的 `0.20.x` 继续迁移；来源已是 `0.20` 或目标达到 `0.21` 仍会失败关闭。

### For contributors

- 新增 GitHub-hosted PR 复杂度门禁：Knip 与 Vulture 使用只读、单调缩小的 68 条基线，新增发现、过期条目或相对 base 扩张都会失败；本地默认自动解析目标分支并在缺失时失败关闭，同文件同名诊断按重数核对，不能通过扩基线或折叠第二处诊断制造假绿。扫描范围显式覆盖 Playwright 支撑代码、仓库脚本与宿主 JSX，动态入口逐个列名，不用通配符掩盖孤儿文件。Knip 独立锁在 `tools/quality` 并使用独立 Node 版本合同，不改变产品 L0 或杭州生产依赖指纹，深度扫描只生成报告、不自动删除代码。
- L0 覆盖五份 Windows 脚本的原生命令退出码顺序、预事务清理边界和 legacy 版本门；真实 PowerShell 5.1、WinSW、Session 1 Illustrator 与发布恢复仍须在合入后的杭州 L1 验证，Mac 通过不等于已上线。
- 文档地图现已区分当前合同、部分被替代设计与纯历史记录；`DESIGN.md` 同步为网页打样固定走 Packaging Structure V2，并写清旧 AI 只能生成待管理员确认的刀线候选。

## [0.20.0.0] - 2026-08-28

### Added

- Windows 新增 InteractiveToken Session 1 Illustrator 桌面代理：LocalSystem worker 与自托管 runner 只通过受 ACL 保护的 UTF-8 JSON 命名管道请求既有 VBS/唯一 JSX，不再从 Session 0 拉起隐藏 Adobe 实例。
- 代理心跳绑定交互用户 SID、脚本 SHA-256、发布版本、Git checkout 与命名管道；客户端同时核对心跳 PID 和实际管道服务 PID，可信 `main` 发版在重新开放业务前验证当前 checkout 的完整 Session 1 管道合同。

### Changed

- 打样开工在消耗上传回执前检查桌面代理；Agent 接单后验证同会话可见窗口、配置中的 Illustrator 可执行路径和空文档列表，并以单一绝对期限覆盖预热、COM、JSX 与清理。
- 杭州发版在切换 8787 前先通过本机随机令牌 + 2 分钟续租 admission drain 原子停止动态页面和全部业务 API 请求（含 SPA 会话检查及会清理 session/OAuth 状态的 GET），并等待响应正文 close/cancel/error、内存 worker 槽、持久作业、上传、作业通知 outbox 与签字通知全部收尾；不可读任务/打样记录会稳定返回 `jobs_unknown`，不再被跳过。仅静态资源、health 和 release control 豁免。Hono 只向发布脚本返回稳定的 `ready/blocker_codes`。journal/watchdog 落盘后短租约提升为不自动过期的 transaction fence，发版超时、进程中断或重启也不会自行开放接单；只有匹配 `lease_id` 的提交/恢复路径可以解除。旧 0.19 没有 control，首次切换必须在旧 8787 已停的批准维护窗显式传 `-AllowLegacyOfflineBootstrap`，不允许带竞态自动自举。
- Actions bootstrap 改为用 GitHub Contents API 将事件 SHA 的四个发版组件下载到 `RUNNER_TEMP`，再把同一个完整 `GITHUB_SHA` 传为不可变 `TargetSha`，不再在 journal 前 checkout/reset 生产 index；依赖核对、VERSION、journal、Git 锁 owner、ff-only 与最终 HEAD 全部绑定该 SHA，main 后续推进不会让旧协议顺带部署新提交。`package-lock.json` 只恢复工作树 blob且不切分支。停服前建立只允许 SYSTEM/管理员访问的原子发版 journal、旧 UI 哈希快照和独立 SYSTEM watchdog；npm 依赖指纹改由 Node 解析真实 lockfile（兼容 `packages[""]`），并在停服前对 npm 图、tsx、真实 Hono 服务入口、Vite、Rollup、Python 依赖及 worker import 做离线冷启动探针。探针无法启动时不会复用陈旧的 `LASTEXITCODE`。真实依赖变化或本地运行环境不完整会保持旧站在线并拒绝发版。构建、Agent、重启、冒烟、Actions 取消或进程中断都按停机前 SHA/VERSION、UI 快照和 Agent 身份离线恢复，不执行 npm/Python 在线安装或重建旧 UI；恢复先切回旧 SHA，再对实际将启动的旧树重跑离线探针。merge 前不可变记录 owner/operation/ref/TargetSha、全缺失锁基线、精确策略哈希和起始 FILETIME；恢复只处理 index、HEAD/main、ORIG_HEAD 及对应 reflog 的七个精确锁，mutable stage 或后续 `recovery_failed` 不能扩大授权，首个失败来源也不会被覆盖。目标/回滚 UI 与 Agent 身份复核通过后才解除 journal。
- 发版控制新增随机实例身份的 loopback 在线挑战，替代 PID + 宽松时间窗；发版/恢复及 Illustrator 执行改用 ACL 运行目录内的独占文件锁，临时 Agent 请求在抢锁前后都受同一绝对到期时间约束，watchdog 会持续重试到 journal 被解除。
- 目标 Node 在冒烟和事务提交期间继承同一 admission transaction fence，确认提交后才原子开放业务写入；一次性 `0.19→0.20` 维护窗新增两次稳定的持久任务、上传与桌面 worker 离线交接检查。
- Agent 任务升级按管理员 SID 识别当前登录会话，安装和卸载都等待任务及心跳 PID 真正退出；新 checkout 只有在旧进程退出后才注册启动。
- 杭州 self-hosted 生产 runner 只接受 `main` push；删除 PR 冒烟和可选择任意 ref 的手工分发入口。Illustrator L1 改在不可变目标 SHA 已合入、目标服务仍受 transaction fence 拒写时执行，成功后才开放业务。
- 一次性 `0.19→0.20` 离线自举只允许对同一条可信 `main` push 的失败任务执行 **Re-run failed jobs**；首轮仍失败关闭，脚本继续核验精确版本、离线状态和空闲证据。

### Fixed

- 修复 Session 0 Illustrator 崩溃并自动恢复上一单后，后续任务持续误报“还有其他稿件打开”的根因；PowerShell 代理保留系统代码页读取 cscript，再统一输出 UTF-8 JSON 错误合同。
- 修复失败清理被静默吞掉并把代理重新标成 idle、已删除的 PR 冒烟误复用生产代理、嵌套超时相加超出作业预算，以及同名错误进程可能被当成已配置 Illustrator 的问题。
- 修复 cscript 超时后无限 `WaitForExit()`、清理最终 probe 超时却回到 idle，以及发版空闲检查后仍可能接进新单的竞态；无法证明退出或空文档时统一保持 `faulted`。
- Illustrator 执行前新增所有 Agent 实例共用的持久安全围栏；进程崩溃或清理未确认时后续实例也不能越过。围栏只能由交互管理员在确认 Agent、Illustrator 与 cscript 均已清空后显式解除。`0.20+` 回滚代际同样先继承原 release transaction fence，核对版本/control/listener 后才恢复接单。
- 修复旧版 `comparing` 任务重启回收后缺少 `job_kind`、被队列永久计为 `jobs_unknown` 的迁移问题；现在会落成完整的 `compare/failed` 终态。

### For contributors

- L0 覆盖心跳身份、故障保持、结构化错误、缓存失效、发版忙门和生产 runner 隔离；真实 Windows PowerShell 5.1、InteractiveToken、Illustrator COM/JSX 与生产真稿仍由杭州 L1/L2 验收，Mac 绿灯不替代上线。

## [0.19.0.0] - 2026-08-28

### Added

- 审稿和打样创建结果新增跨路由 queued 快照交接；详情页仍立即向服务端重验，避免提交页与等待页重复闪屏，同时不把前端快照当成第二份状态源。
- 审稿核对图新增统一预解码与超时、取消、SVG 失败转高清 PNG 的媒体边界；运行时图片加载失败也会切换同一兜底路径。

### Changed

- 分片上传把调用方取消信号贯穿会话创建、分片写入和完成回执；超时后保留可续传会话，并向用户提供继续上传而不是整份重传。
- 疑点列表优先展示真实问题，一致项单独折叠；`coverage.miss` 即使粗状态为一致也进入人工疑点，原始字段索引保持不变。
- 确认单隐藏行、隐藏备注和职责/签字页脚统一在 Excel 解析边界处理；项目列、内容列被隐藏或过滤后没有可核对字段时明确失败，不生成可签字的假干净单。

### Fixed

- 修复审稿缩略图首次进入模糊、全屏后才变清晰，以及 stale queued 快照吞掉 403、500 或持续网络错误的问题。
- 修复复杂转曲 PDF 被少量分组命令误判为活字稿，以及 Illustrator 结构导出把缺失产物误报成语义错误、把本机路径或凭据带入日志的问题。
- 修复开始审稿、开始打样后晚到响应强制跳转，React 严格模式清掉交接快照，以及浏览器默认请求 `/favicon.ico` 返回 404 的问题。

### For contributors

- L0 覆盖零字段 fail-closed、PDF 分型、Illustrator 错误合同、上传取消/续传、详情交接、核对图降级与疑点分组；真实 Illustrator、Blender 和生产上传仍需杭州 Windows L1/L2，Mac 绿灯不替代上线验收。

## [0.18.0.0] - 2026-08-27

### Added

- 旧包装 `.ai` 可从已识别刀线层提取仅描边、无填充的矩形六面候选，并在真实原稿预览上交由管理员逐面指定正、反、左右、顶、底及旋转方向。
- 新增私有结构原稿预览接口和六面确认合同；候选多边形、方向与源稿哈希保持绑定，确认后的结构才可形成 Blender 清单。

### Changed

- 网页新建打样统一进入包装结构 V2，不再暴露旧引擎运行时开关；缺少可靠刀线、闭合六面或人工确认时停在待确认或不支持状态。
- 六面候选按包装网格的主正交方向归一，并在人工确认后裁掉未选几何，再交给同一严格拓扑解析和既有 3D 流水线。

### Fixed

- 修复旧稿中参考图、重复线和非矩形折翼干扰面选择，导致正面被映射成侧面、正文贴图缺失或整盒只剩纯色的问题。
- 底面与其他五面统一校验贴图来源、方向和镜像；任一面缺图或绑定不一致都会失败关闭，不再交付看似成功的 GLB。

### For contributors

- L0 覆盖矩形候选、旋转刀版、重复线、人工六面确认、私有预览权限和 V2 单路径；真实 Illustrator/Blender 稿件仍需杭州 Windows L1/L2 验证，Mac 测试不替代生产验收。

## [0.17.0.0] - 2026-08-27

### Added

- 审稿台和打样台支持 1 MiB 分片断点续传：网络波动会自动重连，暂停或整页刷新后重新选择同一文件即可从服务器已收位置继续。
- 上传状态统一出现在工作台、看板和历史记录中，明确区分上传中、网络重连、服务器确认、待继续、待开工和失败，并可从对应状态直接恢复。

### Changed

- 两条上传通道在分片间隙仍保留短租约；第三份立即返回 429，暂停超时的会话续传前会重新抢位，杭州发版也会把活跃分片会话计入安全门。
- 待开工回执和部分上传共用服务端列表、账号隔离、30 分钟有效期与容量限制；删除接口改为可安全重试，批量删除会居中展示每条失败原因。

### Fixed

- 修复上传到 100% 后遇到 524 或响应丢失时前端误报失败、无法找回服务端回执，以及完成回执沿用会话起始时间而刚成功就过期的问题。
- 修复暂停上传卡片带品名时误发开工请求、切换页面中断仍在进行的上传，以及发布检查只看瞬时 HTTP 请求而漏掉分片间隙的问题。
- 上传会话元数据新增 16 KiB 边界，并补齐分片哈希、偏移、重复请求、所有者隔离、并发占位和恢复路径的失败语义。

## [0.16.0.0] - 2026-08-27

### Added

- 新增 PDF 入稿 V2：逐页识别活字、转曲与图片稿，并输出受控诊断；新增版面区域和字段证据收敛模块。
- 审稿画布支持中英文品名共享一个联合钉，同时保留两条字段和各自的人工结论。

### Changed

- 活字页直接读取 PDF 文字层；只有转曲或图片页调用一次百度 accurate OCR。混合文档按页合并权威文本和词框，不再依赖整份 PDF 的单一判断，也不做多引擎并框。
- 对照真实阶段扩展为「出图 → 识稿 → 认字 → 分区 → 对照」，看板百分比与等待步骤保持单调。
- 每字段最多保留一个真实命中框和一个警告框；版面上下文只用于选框，不能伪装成字段命中。

### Fixed

- 修复组合刀版尺寸把转曲页误判为活字并跳过 OCR、混合页短 OCR 文本被丢弃，以及跨页警告停在错误页的问题。
- 修复多处警告合成跨页巨框、只有上下文却显示绿色命中框，以及中英文联合框画出两个重叠编号钉的问题。
- 修复旋转 PDF 的文字钉框未随页面转向、单张满版图片、多图平铺或密集转曲与少量活字同页时误跳过 OCR，以及多页共用单页 Y 轴分区导致的错区选框。

### For contributors

- L0 覆盖 PDF 分型、按页 OCR 路由、跨页文本合并、区域选框、单钉预算和前端阶段消费。真实稿准确率仍由人工核定金标与杭州 Windows L2 决定。

## [0.15.0.0] - 2026-08-27

### Added

- 审稿页新增高分辨率矢量核对底图；渲染在独立、限时且有输出上限的子进程中完成，失败时保留 PNG 兜底并记录可诊断原因。
- 打样 V2 新增六面贴图绑定、方向、镜像与底面覆盖校验，Blender 5.2 也能显式创建材质和世界节点。

### Changed

- 审稿台与打样台卡片统一为「品名 + 状态或真实进度」单行明细，日期独占末行；打样台持续展示真实作业阶段。
- 核对窗改为横向置顶布局：疑点列表在上，疑点、Excel 应印、稿上 OCR 三列对照，包装定位和人工结论收拢在下方。
- 二维码按完整扫码关注引导语判断；长文案、使用方法和生产信息改用通用证据定位，不再依赖产品特例。

### Fixed

- 修复核对图在深色主题透明、备注与签字或复制并发时丢失，以及无疑点字段仍显示空错误列的问题。
- 修复紧凑看板丢失实时阶段、核对窗默认位置被误持久化，以及复制改稿清单遗漏未命中证据的问题。
- GLB 严格验证六个已确认面及其贴图来源，避免底面缺图、方向错误或镜像后仍被当成可交付结果。

### For contributors

- L0 覆盖矢量核对面、二维码语义合同、通用长字段定位、核对窗行为和六面 GLB；真实稿视觉与杭州 Windows 仍按 L2/L1 闸门验收，本版本不以本地测试代替生产验证。

## [0.14.0.0] - 2026-08-27

### Added

- 新增包装语义结构 V2：Illustrator 只导出显式 `packaging:cut` / `packaging:crease` 等结构标签，统一进入 `PackagingStructure`、Shapely 拓扑诊断与闭合六面验证；格式适配和几何求解不再散落在 Blender 脚本里。
- 新增管理员六面确认工作流。方向或角色有歧义时展示真实面多边形并停在「待确认结构」，确认结果绑定源稿哈希；不支持或没有显式结构语义的稿件不会静默套用方盒模板。
- 新增 Windows Illustrator COM → 同一份 JSX 的 PR 级 L1 冒烟，以及只读真实语料审计、结构基准、GLB 尺寸/坐标轴和白底图像验证工具。

### Changed

- V2 把结构、印刷 artwork、三维渲染拆成稳定边界：先验证结构，再做精确仿射贴图，最后调用既有 Blender 流水线。旧 `dieline.py` 冻结为迁移回滚基线，没有在原路径继续叠加猜测补丁。
- 发布安全门把正在接收的 `/api/uploads` 纳入杭州本机 health；活跃上传或旧版 `.incoming-*` 尚未稳定时，`release.ps1` 会拒绝停止 8787，避免发版截断 multipart 落盘。
- V2 作为可回滚能力发布，生产设置 `PACKAGING_STRUCTURE_V2_ENABLED` 默认关闭；13 份私有真稿仍待人工结构真值与杭州 Windows L2，通过闸门前线上继续使用既有入口。

### Fixed

- 白底图改为背景独立合成纯白，不再靠全局色彩变换把约 `230,230,230` 抬白；产品贴图保持源稿色调，合成烟测的主色由源 `117,35,46` 保持到约 `115,39,48`。
- GLB 导出后会核对轴向和毫米尺寸，白底视角只使用已确认六面；结构刀线从 artwork 对象级排除，不再烘进产品表面。
- 结构复核、上传确认和作业状态在工作台、看板与历史记录之间保持一致；非终态不会被旧 `job_finished_at` 误判为已结束。

### For contributors

- L0 覆盖语义标签、拓扑残线/悬线、六面确认、路径边界、像素上限、Windows 旧入口与发布上传安全门；E2E 覆盖结构状态和管理员确认。Windows PR 冒烟只是 L1，不替代真实稿 L2。

## [0.13.2.0] - 2026-08-26

### Added

- 新稿支持两份同时流式上传；第三份立即提示等待其中一份完成，不把请求堆到网关超时。
- 历史记录可一键全选当前筛选结果里的已结束记录；进行中的记录始终锁住，桌面和窄屏都保留视口居中的批量栏与删除确认。

### Changed

- 审稿台四个板块统一为紧凑明细：首行只放品名与状态；对照中有真实阶段时显示粗粒度百分比，底部单独放日期。没有 `STAGE` 时仍写“正在对照”，不猜数字。
- 同路径 GET 轮询会合并，暂时性网络错误和 429/502/503/504/524 按 5–60 秒退避；写操作成功后废弃旧轮询和旧退避，避免删除后闪回旧记录。
- 浏览器大稿上传窗口保持 15 分钟，Hono 服务端请求窗口延长到 16 分钟，为落盘和回执留出余量。

### Fixed

- `/api/uploads` 改为 multipart 直接流式落盘，不再经 `parseBody()`、`File.arrayBuffer()` 和 `Buffer.from()` 复制整包，避免接近 100 MB 时内存放大并拖垮 `/api/status`、`/api/tasks` 等接口。
- 上传截断和待开工配额现在分别返回可执行的 400 / 429 中文原因，不再被包装成通用 500；中断与校验失败会清理临时文件。
- 待审核和已签字卡不再沿用机器作业的 `100%`；固定 `@google/model-viewer` 4.2.0，构建产物不再带 `$updateSource` / `BAILING OUT EARLY` / `IntersectionObserver fired` 调试串。

### For contributors

- 新增流式 multipart 失败清理、配额状态、服务端上传窗、GET 写后失效与退避上限测试；浏览器回归覆盖审稿台紧凑卡、历史全选、筛选/实时刷新裁剪和双视口居中。

## [0.13.1.0] - 2026-08-26

### Added

- 审稿台和打样台都有上传中、服务器确认中、待开工与失败状态；切去别页再回来仍能看到，回执落盘后整页刷新也能从台面恢复，本次上传响应丢失时会按唯一回执标识自动找回。
- 打样台补齐品名/文件名搜索、看板与表格模式。历史记录可按类型、时间、生成人筛选，并在权限允许时批量删除已结束的单。
- 上传页增加「放弃上传」：释放浏览器文件引用，已落盘的待开工回执也会一起删除。

### Changed

- 核对窗用整页 portal 置顶，可拖动、八向缩放和全屏；桌面左列是当前字段（疑点/错误点、Excel 应印、稿上 OCR），右列疑点列表可自由换行，窄屏按疑点优先纵排。
- 工艺说明、颜色要求、版本号、更新内容不进机审；“备案版本号”“执行标准版本号”等业务字段仍保留。
- 审稿上传总量固定不超过 100 MB；服务端同时限制解析并发、每人/全局待开工数量与字节数、30 分钟过期，并把打样历史元数据缓存封顶为 256 条 LRU。
- 公网 health 不再暴露队列人数与飞书开关；精确作业状态改由杭州本机 health 和登录后的 status 提供。

### Fixed

- 修复文件进度到 100% 后一直没有结果：100% 现在明确进入「服务器确认中」，收到落盘回执才变为可开工；正常断网响应丢失不会重复建单。
- 同名文件反复上传各自独立；ready 后释放原始 `File`，更换其中一份不会保留无法提交的幽灵文件名。
- 只读账号不再因无权读取上传回执而看不到审稿台/打样台；删除按钮和接口统一按真实 `delete` 权限。
- 对红重新进页默认显示第二版；`hits_v2` 为空时回退第一轮字段。慢速上传改稿期间并发删单不会把残缺任务复活，低于 100 MB 的自定义上传上限仍生效。
- 设置写入仅管理员可用；签字会在最后落盘前重读作业状态，避免与正在入队的对红互相覆盖。
- OAuth state、会话、worker 输出和持久化 PID 接管都增加上限或归属校验；L0 通知测试在生产发送边界失败关闭，不会因本机凭据误发飞书消息。
- 已结束任务在飞书通知发送期间被删除时，晚到通知不再复活记录，也不会产生未处理异常拖垮服务。
- 上传井和历史记录行支持键盘操作，上传回执恢复、批量删除保护、核对窗全屏与全屏失败都补了浏览器行为回归。

### For contributors

- 新增 `npm run test:e2e`，用 Vite + 合成 API 覆盖上传、历史记录和核对页交互；真实 Hono 与 Windows worker 仍由产品冒烟和杭州 L1 验证。
- 金标评测在没有可评案例时非零退出；`dev-start.sh` 按 workspace 实际依赖检测，只会在缺依赖时执行一次安装。

## [0.13.0.0] - 2026-08-25

### Added

- 审稿台独立地址：看板 `/reviewup`，工作台 `/reviewup/new`。核对页仍是 `/review/:id`。旧 `/`、`/new`、`/review` 会转到新地址。
- 先把文件传到服务器（能看见进度），回执在了再开工。超 Cloudflare 100MB 的文件在点开始前就会拦住。
- 审稿台四格、打样台三列：列高锁住，卡多了列里滚；右上「全部」打开这一列的明细表。
- 外观按飞书号分开记，换人登录不会带上别人的主题。

### Changed

- 「待她判」改成「待审核」。
- 打样台看板和工作台分开（`/mockup` / `/mockup/new`）。打样单页头只下 PPT，不再给 PDF 链接。
- 设置里的接口分组走 `/settings?group=`。
- 新单的主人写飞书号；卡片仍显示花名。没有主人的旧单，非管理员打不开。

### Fixed

- 公网 413 HTML 不再当成「审稿服务没回上」。
- 上传回执绑飞书 `open_id`，同名两人不能互相开工。
- 白底静帧正反面旋转之后会按新外框重算相机，高瘦盒不再把盖子裁掉。
- 开工回执号必须是 12 位十六进制，不能拿去拼磁盘路径。拷进任务目录后清掉暂存。换文件会停掉还在传的那次。

### Removed

- 开工只走「先上传再开始」。旧的一次 POST 上传口拿掉了。
- 退役的原生页 `apps/web/frontend` 删掉了。
- Python 不再提供 FastAPI 网页。对照只走命令行 worker。

## [0.12.21.0] - 2026-08-25

### Changed

- 核对页的包装图按页比例缩进画布，停在右边，左边留给核对窗。放大缩小和点序号跟框，仍对准该字段。

### Fixed

- 本机只开 Vite `:5173`、审稿服务没开时，开始打样 / 对照会明确说「本机 :8787 没开」，不再把 HTML 当成已经入队。
- 公网若 `/api` 没回 JSON，只说「审稿服务没回上」，不会再叫人去开 Vite 或本机 8787。

## [0.12.20.0] - 2026-08-25

### Changed

- 核对窗脱离页面随便拖，页图不再被挤到一边。窗仍叠在侧栏上，夹住不盖页头签字。
- 打样单三张图同一高度。下载在每张图右上角。页头写「下载+PPT」，有合成 PDF 时旁边还能下 PDF。
- 点下载底部出玻璃条「正在下载…」，点不着、不挡手。
- GLB 全屏后模型左右上下居中。截图时下载钮藏起来，全屏被拒会说打不开。

## [0.12.19.0] - 2026-08-25

### Changed

- 核对窗仍叠在左侧栏上。页图和放大/钉工具进窗右边剩余区，不再和窗抢点击。窗可以盖住画布，但夹住不盖页头签字。
- 当前字段顶栏有 miss 或她标了有错时，先出现「疑点 / 错误点」，再是 Excel 应印。一致且无 miss 不占地方。
- 条码和净含量按包装上的规格标签审（5片 / 单片 / 30ml装），配方里的 2ml+28ml 不配码。当前规格命中一条条码即过。
- 打样下载改成图下说明行右侧 44 玻璃圆钮，不压产品。PDF 槽按源图比例 contain。棚只提亮产品和接触阴影，不改盒子材质。

### Fixed

- 成分表钉不再跨到刀模邻板。
- 规格对不上时不再用稿上已印的码自证「一致」。
- 选膜袋时片数规格跟着改成单片。

## [0.12.18.0] - 2026-08-25

### Changed

- 进核对页时核对窗是展开的，叠在左侧栏上，不盖右边签字。点收起/展开有 280ms 动画，不再瞬间换成按钮。页头签字和画布上的放大/钉仍点得到。
- 打样台和打样单是白底。GLB 也是白底，灯光收一点，盒子边比原来清楚。已经出过的白底图要重新打样才会变白。

### Fixed

- 打样 PDF 先铺白再贴图。pymupdf 写不出时用已经装好的 Pillow 再写一次，不用再下插件。旧单没有 PDF 的，重打一次就会出现下载。

## [0.12.17.1] - 2026-08-25

### Fixed

- 杭州发版：PowerShell 不再把 `git fetch` 的 stderr 当成失败。Actions 用 cmd 先拉码并检出新的 `release.ps1`，盘上旧脚本坏了也能升上去。

## [0.12.17.0] - 2026-08-25

### Added

- 浏览器地址按台分开：`/` 审稿台、`/new` 新建、`/review/:id` 核对页、`/mockup` 打样台、`/mockup/:id` 打样单、`/history`、`/settings`。后退换台，不再钉在 `/`。
- 打样单同一屏左到右三图：正面+侧面、反面+侧面、GLB；每张右上角下载；「返回打样台」下面下载 PDF。
- 打样 PPT 用两张白底直接写成文件，不依赖 Node。没有 PPT 时整单仍算打样成功。

### Changed

- 核对窗钉到整页最上面（含侧栏），可拖到左边。窄屏抽屉仍盖过核对窗。
- 核对页「稿上 OCR」同时列出命中、未读到的短语，以及命中项数。对照算法没改，只把列表从 8 条放到 36 条。品名 / 品牌 / logo 没字时说明稿上多半是图。
- 密折痕花盒刀线先密后疏试间距，折得出盒面再停；已能折的大方盒不改。光栅读不出时才描刀线路径再折一次。
- 两张白底合成一页 PDF。标题用中文字体。缺 PPT 或 PDF 时不假装能下载。

## [0.12.16.0] - 2026-08-24

### Added

- 核对窗可从上下左右和四个斜角缩放，默认停在页头下面，不再盖住签字。
- 打样台只交稿和列单；点进度或已出图进入单独打样单，可看白底、全屏转 GLB、下载 PPT。

### Changed

- 打样 PPT 改成一页两张白底：正面+侧面、反面+侧面。

### Fixed

- 杭州 `git fetch` 若 `origin/main` 指针拧了，只删这一条再拉，不再因网络失败误删 tracking ref。
- 打样下载不再把 `ai-raster` 或 PPT 质检图当成白底；旧单里误标的白底也会拒绝。坏 PNG 直接说明不是图。
- 打开打样单不再把整张白底和 GLB 一次读进内存；预览 inline、点下载才附件。进行中的打样单也能返回打样台。

## [0.12.15.0] - 2026-08-24

### Added

- 核对页「显示框」和「显示钉」分开开关；工具条可收缩，拖动画布时框暂时藏起来。
- 核对窗钉在整页右上，可拖、四角缩放，不再叠在图里。
- 历史记录列出进行中的审稿和打样，并写阶段和大约还要多久；点进去仍看等待卡。

### Changed

- 新对照的包装 PNG 按更高分辨率出图，局部放大看小字。
- 确认单底部工艺说明、颜色要求、版本号不再进机审。

### Fixed

- 打样找不到 Node 或演示文稿运行时时跳过 PPT，白底图和立体模型仍能出。不再把整单判成「Node不存在」。空的 ppt `node_modules` 也跳过。
- 旧单里工艺说明 / 颜色要求 / 版本号的假疑点不再挡住签字。只认这三项字段名开头，不会把「执行标准版本号」藏掉。
- 高清出图先按最长边缩到上限，不再先把整页撑进内存。核对页页头压在悬浮窗上面，签字点得到。

## [0.12.14.0] - 2026-08-24

### Added

- 打样先读稿上的刀线/刀版，按展开图还原长宽高再出 3D。5 片/7 片花盒、膜袋不必再套 47.5 方盒。
- 核对窗可拖左下角放大缩小，材质是液态玻璃。

### Changed

- 核对页拖动画布不再弹出地球图标；拖动和滚轮缩放走 rAF，松手或取消指针才写回状态。
- 「隐藏钉」只藏编号圆圈，定位方框留下。
- 签字结论写在页头，不在悬浮窗底。

### Fixed

- 刀线读不出或没有刀线层时失败写清，不再把方盒硬套到别的刀模上。
- 刀线按 MediaBox 读，细 CropBox 不再把展开图裁没。
- 打样栅格有像素上限，瘦高刀版不会把杭州打爆。
- 膜袋只还原正反面，缺侧面不再当成切面不完整。
- 刀线还原超出化妆品盒尺寸就失败，不再出离谱 3D。
- 一页两套相近展开图先按花盒折，不再误当成 3mm 膜袋。

## [0.12.13.0] - 2026-08-24

### Changed

- 核对页右栏改成可收起的悬浮窗，字段和疑点两列并排，画布吃满宽度。
- 图上的编号钉可以关掉，避免挡住稿面。

### Fixed

- 打样失败不再把整段错误塞进标签把看板挤竖。标签只写「打样中断」，原因折行且不带 Windows 路径。
- 画板和登记花盒对不上时，说明只接 47.5×47.5×177.5mm 方形花盒。

## [0.12.12.0] - 2026-08-24

### Added

- 核对页疑点带 1 2 3 4 序号，点一条左边稿面放大到这一框。
- 左图可滚轮、拖动、放大缩小复位；中间拖条改左右栏宽。
- 对照/对红/打样等待卡换成 Generating 圆盘，字是对照中 / 对红中 / 打样中。

### Fixed

- 稿上 OCR 会显示命中的字，不再空白。
- 疑点列表超长可滚动，不再挡住右栏。

## [0.12.11.0] - 2026-08-24

### Added

- 核对页在稿上画矩形框，编号钉在框中心；没有位置就不画假钉。
- 离开审稿台或打样台再回来，看板卡片和侧栏圆点能看到还在跑的阶段。
- 打样流水线打出「出图 / 打样 / 导出」，等待卡芯片跟着走。

### Fixed

- 杭州打样不再依赖 macOS Quick Look：平面用 pymupdf 出图。
- 细 CropBox 的平面稿按整页出图，不再把窄条放大到撑爆内存。
- 已结束的打样单不会再被当成正在跑，把等待卡锁死。

## [0.12.10.0] - 2026-08-24

### Fixed

- 打样能过缺库秒退：对照用的 Python 补上 `pypdf`，杭州升版会装进同一个解释器。
- 对照失败后核对页仍能看到已经出的图，并显示短错误，不再只写「对照中断」。

## [0.12.9.0] - 2026-08-24

### Added

- 选 Excel 或平面稿后自动填品名（去掉【】和扩展名），上传井显示已选文件。
- 打样台也有看板：打样中 / 失败 / 已出图。
- 历史记录改成表格，可删已结束的单。

### Changed

- 审稿看板把对照失败从对照中拆出来。狐狸轨道改缓动，进度条不再按 40 秒填满。

### Fixed

- 点已经中断的对照单不再闪「对照中 40 秒」。
- 对照还在跑时不能从历史记录删除，避免卡住 OCR/Blender 槽。

## [0.12.8.1] - 2026-08-24

### Changed

- 代理按 `AGENTS.md`「易忘约定」开工：公网画面不是未合分支、没叫 `/ship` 不出 PR、飞书推送不必装 lark-cli、魏炜才是管理员。MICRO 升版不改 `package.json` 三位。禁止进仓的杭州切流稿写入 `.gitignore`。

## [0.12.8.0] - 2026-08-24

### Fixed

- 杭州升产失败时不再假回滚：回到停机前的 SHA，缺依赖或缺前端就在旧树上重装重编，再拉起 8787。Actions 超时/取消也会 `schtasks /Run`。`npm` 前清掉 `GITHUB_TOKEN`。锁文件没变则跳过 `npm ci`。

## [0.12.7.0] - 2026-08-23

### Added

- 魏炜飞书登录即为杭州机箱管理员，扫描 Blender / Illustrator 会搜 PATH。
- 飞书推送不必装 lark-cli：用已填的飞书应用发给当前登录。点「发一条测试」会自动打开推送并记下 open_id。没有飞书身份会先带去授权。

### Changed

- 开工板「发一条测试」有发送中、行内说明和 toast。百度 OCR、MiniMax 已通的密钥默认收起，点开才改。MiniMax 未开语义复核显示「未用」，不再画成「可用」。

## [0.12.6.0] - 2026-08-23

### Changed

- 打开审稿台不再先下一整包打样 3D。主脚本大约 900KB，点打样台才加载 model-viewer。未登录从公网进来会先去飞书，不必等 SPA。源站给 JS gzip；带 hash 的 `/assets` 可长期缓存，HTML 每次重验。

### Fixed

- 公网未登录的 302 带 `private, no-store`，避免边缘把跳登录缓存给已登录的人。

## [0.12.5.0] - 2026-08-23

### Changed

- Mac 合并闸 `npm test` 不再访问飞书外网，也不再把已退役的 FastAPI 登录/上传当产品测试。默认只测 Hono 审稿服务、对照 CLI 契约、界面纯函数。杭州生产机仍只做 health 和 logo 冒烟。

### Fixed

- 对照/打样单测共用临时目录时，上一例留下的任务文件会串进下一例的排队。现在每例清盘；磁盘上已经删掉的打样单也不会再从内存缓存里读回来。

## [0.12.3.1] - 2026-08-23

### Fixed

- 杭州 runner 的 PowerShell 执行策略 Restricted 时，GitHub Actions 生成的临时 `.ps1` 会 1 秒红。workflow 改走 `cmd`，再用 `-ExecutionPolicy Bypass` 跑 `release.ps1`。

## [0.12.3.0] - 2026-08-23

### Fixed

- 合进 `main` 自动升杭州时，不再因为 Windows `npm` 改脏的 `package-lock.json` 把公网打成 502。脚本先丢掉锁文件再停 8787；还有别的本地改动就保持旧站。安装改走 `npm ci`。pull 或编译失败且 8787 没听时，把上一版计划任务拉回来。workflow 调磁盘上的旧脚本前也会先丢掉脏 lockfile。

## [0.12.2.0] - 2026-08-23

### Added

- 打开设置 → 外观，侧栏材质可改实心、毛玻璃、液态玻璃。液态玻璃更透，能看见壳上的紫雾，带高光边和暗边。旧的「半透明侧栏」开关会迁到毛玻璃；关掉的迁到实心。系统开了「降低透明度」时，毛玻璃只降模糊，液态玻璃（浅色和深色）底更实。窄屏 ≤720 展开侧栏盖在主区上时，液态玻璃改走更实的雾面，避免底下的字糊进侧栏。

### Fixed

- 杭州 `release.ps1` 的合同测试跟着 `Invoke-Git pull` 走，不再找已经不存在的裸 `git pull` 字样。

## [0.12.1.0] - 2026-08-22

### Changed

- 打样台平面稿只收 `.ai`。拖 PDF 会提示「只收 .ai 稿件」，接口同样 400。没有 Illustrator 仍是 412，不会先把文件读进内存再拒绝。

### Fixed

- 侧栏展开时「SHINE MAGE」不再先闪出一半。词标始终一行，宽度够了再淡入。
- 侧栏玻璃能看出来：灰紫雾面，主区白紫台。关掉半透明是实心灰紫，不是白底。
- 新建 Excel↔PDF 的双井在宽屏撑满剩余高度；窄屏上下堆、自己滚，不再叠在步骤卡上。

## [0.12.0.3] - 2026-08-22

### Fixed

- 杭州下次升版时，中文 Windows 的 PowerShell 5 能直接跑 `release.ps1`（文件带 UTF-8 BOM）。`npm install` 之后若缺 Windows 的 Rollup 原生包会先补上再编 UI，避免 8787 已停、页面编不过。

## [0.12.0.2] - 2026-08-22

### Added

- 杭州生产机升版改走仓库里的 `scripts/windows/release.ps1`。对照、打样或 Illustrator 在跑或排队会失败。脚本先停监听 8787 的那棵进程树（`taskkill /T /F /PID`，不杀全部 node.exe），再 `git pull --ff-only` 并启动。公网会空几分钟。health 不通且还有 worker 不会当空闲。合进 GitHub 仍然不会自动升杭州。

## [0.12.0.1] - 2026-08-22

### Fixed

- 杭州 Windows 上侧栏和拒绝页的狐狸标、导航图标不再 404。品牌图本来就在仓库里；服务器在 Windows 上把 `/brand` 目录多拼了一层。拉到本版后重启 Node，打开首页侧栏应能看见标；`http://127.0.0.1:8787/brand/logo-mark.png` 应返回 PNG。

## [0.12.0.0] - 2026-08-21

### Added

- 设置 · 开工板改成分组清单：七行可用/还缺、直线进度、「全部检测」串行。魏炜 60 秒能看出下一步。
- 飞书登录 / 百度 OCR / MiniMax 打开向导落到现有分组，带编号步骤。保存后回开工板只复测这一行。
- 管理员可「扫描这台电脑」（白名单、8 秒、不执行找到的文件）。候选路径要点「采用」才写入。
- 打样上传 `.ai` 走独立 Illustrator 槽（假 COM 合同）。没有路径会中文拒绝，不会当 PDF 吃进去。真 Windows COM 本波不做。

### Changed

- MiniMax 探测写明 GET `/v1/models` 模型列表，不是对话。7/7 文案是「七条探测通过」，不是可以接稿。
- 飞书 OAuth 从开工板发起的登录会回到开工板。测试推送发给当前登录，不是籽烨。
- ≤720 设置分组改成顶上 Segmented。一屏只留一颗实心紫。

### Fixed

- 探测网络断开不再显示英文 Failed to fetch。

## [0.11.0.1] - 2026-08-21

### Fixed

- 侧栏折叠后，台签图标落在 48px 选中环正中，不再被标签残宽挤偏。
- 新建页「平面稿」井和 Excel↔PDF 药丸文字垂直居中。

## [0.11.0.0] - 2026-08-21

### Breaking

- `python -m app.cli compare|rework` 不再 `save_task`；stdout 最后一行是结果 JSON；`STAGE` 打在 stderr。
- 上传 / 对红 / 打样 POST 立即 200（`comparing`/`queued` 或 `running`），不再等 worker 结束；对照失败不再用 5xx/504 挡在 POST 上。
- 打样忙不再 409「一次只能跑一单」，改为入队；缺 Blender 仍当场 412。
- 非 admin 只能读自己的审核单 / 打样；杭州旧 JSON 空 owner 仍放行。
- 杭州：看板没有「对照中」再 pull / 重启。开机把没有 `job_status` 的旧「对照中」标成对照中断。

### Added

- 对照 / 对红 / 打样共用进程内作业：OCR 槽=1、Blender 槽=1，FIFO 写在任务 JSON 上。
- WaitCard 2.5s 轮询；排队只写「前面还有 N 单」；≥60 秒写分钟。没有假取消，没有「AI 已过审」。
- 作业终态飞书叫人。打样深链目前仍走 `/?task=`（已知洞）。

### Changed

- 侧栏与主区连成一块 28px 圆角工作场，0.5px 发丝分隔；玻璃层 280px 只裁切不缩放。
- ≤1024 默认折叠到 76px；≤720 展开为遮罩抽屉，不挤主区。锁定稿：Figma `Web · 锁定稿`。

### Fixed

- `queue_ahead` 不再按行全盘扫 JSON。
- 对红进行中再 POST 是 409，不会被 `comparing` 误成 400。
- 打样 id 必须是 12 位 hex；产物路径不出 `mockups/{id}/`。
- worker 的 `status: completed` 不能跳过签字门。

## [0.10.2.0] - 2026-08-20

### Added

- 历史记录侧栏 tab：审稿单和本机打样作业混排。
- `GET /api/mockups` 列出本机打样作业，JSON 不带磁盘路径。
- 设置 · 外观：浅色/深色/跟随系统、13–28px 可手写字号、半透明侧栏、侧栏雾面对比度滑条、核对页差异标记。只写这台浏览器，不进密钥文件。

### Changed

- 工作场从飞书顶栏换成 iOS 27 玻璃侧栏（审稿台 / 打样台 / 历史记录 / 设置）。
- 页底淡紫雾、主区独立淡白工作台、侧栏 Liquid Glass。滚动锁在主区，设置页不再双滚动卡顿。
- `DESIGN.md` 锁定 Figma Web 稿，主色 `#805898`。新建左右双井，核对页一对一，对照/打样用等待卡。
- 深色主题走品牌紫雾（不是灰黑中台）。侧栏账户 36px / 15px，图标 24px。

### Fixed

- 初次点审稿台不再闪三栏空看板。加载失败时「再试一次」会真的再拉一次。
- 深色主题打开时少闪一帧浅色。

## [0.10.1.1] - 2026-08-20

### Changed

- 代理约定只留 `AGENTS.md`。打开仓库不再要读一份重复的 `CLAUDE.md`。

## [0.10.1.0] - 2026-08-19

### Added

- 仓库写入深模块约束（ADR-004）：Hono 是唯一 HTTP 入口，对照和 3D 藏在窄 CLI 后面。

### Changed

- 本机 Vite 缓存和 `.claude` 软链不再出现在待提交列表。

### Fixed

- 对照失败或已完成的单不能再签字；非法审核结论会被拒绝。
- 飞书授权失败回到飞书，不再把远程验收人送到本机 `:8787`。
- JSON 404（任务不存在等）不再误报成 Vite 代理挂了。
- 对红和签字分开：签完「待设计改稿」可以传第二份 PDF；干净签字单不能再对红。
- 对红后的疑点写在第二轮字段上，未处理不能签字。
- 本机 5173 开发页把 `/api` 转到 8787，登录不再闪。

## [0.10.0.0] - 2026-08-19

### Added

- TypeScript 审稿室：Hono 对外服务 + React 工作台，本机设置覆盖密钥文件。
- 设置里的费用账单：本机对照台账，加百度月账单/余额和 MiniMax 余量快照（进入再拉，不轮询）。
- 开工板：登录 / 审稿 / 打样能不能干活一眼能看出来。
- 审核单看板：对照中 / 待她判 / 已签字。

### Changed

- 登录不再做自建页。未登录直接去飞书官方授权。
- 顶栏用完整 SHINE MAGE 标，设置收到右上角姓名菜单。
- 出站请求直连飞书/百度，避开本机代理证书坑。

### Fixed

- 只放行伸美企业的飞书号；显示名「管理员」不会变成系统管理员。
- 对照失败会记台账，但台账写失败不会把已经成功的对照打成失败。
- 走飞书官方 authorize + PKCE，去掉假扫码。
- 设置写入限审核员以上；签字推送失败不挡她提交。
