[pytest]
pythonpath = .
testpaths = tests
norecursedirs = .* build dist venv .venv node_modules
markers =
    real_artwork: private artwork, requires --run-real-artwork even when samples exist
    native: native application execution, requires --run-native
